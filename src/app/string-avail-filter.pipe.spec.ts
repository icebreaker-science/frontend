import { StringAvailFilterPipe } from './string-avail-filter.pipe';

describe('StringAvailFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringAvailFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
